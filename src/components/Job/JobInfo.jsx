import React from "react";

const jobInfo = {
  backgroundColor: "#fff",
  width: 450,
  borderRadius: "50px 0 0 50px",
  boxShadow: "rgba(0, 0, 0, 0.24) 0px 3px 8px",
  position: "sticky",
  top: 0,
  right: 0,
  bottom: 0,
  height: window.innerHeight,
};

const JobInfo = () => {
  return <div style={jobInfo}>cao</div>;
};

export default JobInfo;
